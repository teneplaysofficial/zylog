---
title: streams
description: Returns the currently active file streams.
---

## Signature

```ts
get streams(): string[]
```

## Description

Returns a list of file paths associated with active file write streams.

## Example

```ts
console.log(zylog.streams);
// Output: ["logs/zylog.log"]
```
